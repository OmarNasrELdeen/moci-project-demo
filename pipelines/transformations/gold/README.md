# gold/

One `.py` file per Gold materialized view, e.g. `sales_by_store_month.py`,
`top_products.py`, `customer_lifetime_value.py`. Each joins/aggregates
Silver tables into a business-level materialized view.

Not built yet.
