# explorations/

Ad hoc notebooks for exploring Silver/Gold pipeline output. These are
intentionally kept outside `transformations/` so they are never picked up
as part of the Lakeflow Declarative Pipeline's DAG.

Not used yet.
