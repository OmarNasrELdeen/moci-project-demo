# utilities/

Shared helper code reused across multiple files in `transformations/`
(e.g. a common SCD Type 2 merge helper used by several Silver table files).
Automatically importable by pipeline transformation files without any
packaging step.

Not built yet.
